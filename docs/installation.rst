============
Installation
============

At the command line::

    $ easy_install django-powerbank

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-powerbank
    $ pip install django-powerbank
